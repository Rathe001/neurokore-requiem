[gd_resource type="Resource" script_class="EnemyClass" load_steps=4 format=3]

; Ranged + LMG — sustained suppressive fire from mid-range. Longer bursts than
; SMG (6 rounds vs 4) with a longer pause between volleys. Kites at moderate
; distance and pins the player down. The "wall of bullets" archetype.

[ext_resource type="Script" path="res://scripts/enemies/enemy_class.gd" id="script"]
[ext_resource type="PackedScene" path="res://scenes/prototype/prototype_projectile.tscn" id="projectile_scene"]
[ext_resource type="Resource" path="res://resources/enemies/skills/lmg_attack.tres" id="basic_attack"]

[resource]
script = ExtResource("script")
id = &"ranged_lmg"
label = "LMG Gunner"
weapon_id = &"lmg"
basic_attack = ExtResource("basic_attack")
skill_pool = []
attack_mode = 1
attack_range = 14.0
attack_cooldown = 2.0
attack_windup = 0.2
attack_damage_mult = 0.5
projectile_scene = ExtResource("projectile_scene")
projectile_speed = 20.0
projectile_max_range = 16.0
accuracy = 0.60
ranged_kite_distance = 9.0
projectile_is_bullet = true
support_role = 0
