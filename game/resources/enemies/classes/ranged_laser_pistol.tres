[gd_resource type="Resource" script_class="EnemyClass" load_steps=4 format=3]

; Ranged + laser pistol — accurate, moderate pace. The reliable mid-range
; shooter that punishes lazy positioning. Fires slower and harder than the
; Laser Trooper variant (ranged_laser) using the same weapon.

[ext_resource type="Script" path="res://scripts/enemies/enemy_class.gd" id="script"]
[ext_resource type="PackedScene" path="res://scenes/prototype/prototype_projectile.tscn" id="projectile_scene"]
[ext_resource type="Resource" path="res://resources/enemies/skills/laser_pistol_attack.tres" id="basic_attack"]

[resource]
script = ExtResource("script")
id = &"ranged_laser_pistol"
label = "Laser Gunner"
weapon_id = &"laser_pistol"
basic_attack = ExtResource("basic_attack")
skill_pool = []
attack_mode = 1
attack_range = 14.0
attack_cooldown = 1.2
attack_windup = 0.25
attack_damage_mult = 0.7
projectile_scene = ExtResource("projectile_scene")
projectile_speed = 28.0
projectile_max_range = 16.0
accuracy = 0.82
ranged_kite_distance = 8.0
support_role = 0
