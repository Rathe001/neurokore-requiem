[gd_resource type="Resource" script_class="MonsterAffix" load_steps=2 format=3]

; Frenzied: faster swings + faster movement, same damage. Reads as
; "aggressive berserker" — pressures the player into kiting.

[ext_resource type="Script" path="res://scripts/enemies/monster_affix.gd" id="script"]

[resource]
script = ExtResource("script")
id = &"frenzied"
label = "Frenzied"
health_mult = 1.0
damage_mult = 1.0
attack_cooldown_mult = 0.65
move_speed_mult = 1.3
ring_tint = Color(1.0, 0.4, 0.15, 1.0)
weight = 100
min_level = 1
effects = []
