[gd_resource type="Resource" script_class="EnemySkill" load_steps=2 format=3]

; Leap Attack — gap-closer cone attack. The enemy lunges to melee range
; and swings on arrival. Long range lets it close distance; cone damage
; on landing. Higher priority than ground slam so a distant enemy leaps
; before trying to AoE.

[ext_resource type="Script" path="res://scripts/enemies/enemy_skill.gd" id="script"]

[resource]
script = ExtResource("script")
id = &"leap_attack"
display_name = "Leap Attack"
targeting_mode = 0
damage_mult = 1.3
skill_range = 7.0
cooldown = 8.0
wind_up = 0.5
priority = 0
cone_deg = 90.0
knockback = 4.0
min_level = 3
weight = 80
