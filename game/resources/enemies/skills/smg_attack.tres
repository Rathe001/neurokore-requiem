[gd_resource type="Resource" script_class="EnemySkill" load_steps=3 format=3]

; SMG basic attack — 4-round burst of inaccurate fire. Each burst sprays
; rapid rounds over ~0.3s then pauses for the full cooldown. Low per-shot
; damage compensated by volume. Mirrors the player SMG's rapid-fire feel.

[ext_resource type="Script" path="res://scripts/enemies/enemy_skill.gd" id="script"]
[ext_resource type="PackedScene" path="res://scenes/prototype/prototype_projectile.tscn" id="projectile_scene"]

[resource]
script = ExtResource("script")
id = &"smg_attack"
display_name = "SMG Burst"
targeting_mode = 2
damage_mult = 1.0
skill_range = 10.0
cooldown = 2.0
wind_up = 0.3
projectile_scene = ExtResource("projectile_scene")
projectile_speed = 22.0
projectile_max_range = 12.0
projectile_count = 1
burst_count = 4
burst_delay = 0.08
