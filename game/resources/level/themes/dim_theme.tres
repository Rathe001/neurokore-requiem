[gd_resource type="Resource" script_class="LevelTheme" load_steps=4 format=3]

; Industrial-cladding theme for the chain's middle section. Currently uses
; the same PBR materials as tech_theme — when more Blenderkit textures are
; imported, swap floor_material / wall_material here to a darker / cooler
; variant so the dim chain rooms read as distinct from the lobby.

[ext_resource type="Script" path="res://scripts/level/level_theme.gd" id="1"]
[ext_resource type="Material" path="res://resources/materials/floor_metal_panels_displaced.tres" id="floor_mat"]
[ext_resource type="Material" path="res://resources/materials/wall_metal_panels.tres" id="wall_mat"]

[resource]
script = ExtResource("1")
fps_fog_density = 0.35
fps_fog_color = Color(0.005, 0.005, 0.006, 1)
wall_height = 4.5
wall_thickness = 0.4
wall_material = ExtResource("wall_mat")
floor_material = ExtResource("floor_mat")
pit_depth = 5.0
fluorescent_housing_size = Vector3(1.2, 0.25, 0.1)
fluorescent_tube_size = Vector3(1.05, 0.12, 0.03)
fluorescent_housing_color = Color(0.10, 0.11, 0.12, 1)
fluorescent_housing_metallic = 0.5
fluorescent_housing_roughness = 0.5
fluorescent_emission_multiplier = 36.0
