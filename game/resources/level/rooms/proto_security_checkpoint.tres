[gd_resource type="Resource" script_class="RoomDef" load_steps=3 format=3]

; Security Checkpoint — guard station with booth-wall partitions.
; Sparse clutter — this was a controlled area, not a ward. Clean
; and austere.

[ext_resource type="Script" path="res://scripts/level/room_def.gd" id="1"]
[ext_resource type="Resource" path="res://resources/level/lights/fluorescent_white.tres" id="light"]

[resource]
script = ExtResource("1")
id = &"proto_security_checkpoint"
size = Vector2(12, 8)
openings = [3, 2]
opening_width = 4.0
light_color = ExtResource("light")
enemy_count = 2
decorative_pillars = [Vector2(-2, -1.5), Vector2(2, -1.5)]
decorative_pillar_size = Vector2(1.5, 0.8)
clutter_density = 1
